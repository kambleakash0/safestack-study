experiment_id: c3_smoke
description: "Phase-2 C3 guardrail-seam smoke: fixture suites through the mock policy + mock output
  guardrail (ADR-0009). Proves the config -> CLI -> guardrail -> trace path with no VRAM."
condition_id: C3
model: mock
suites: [harmful_fixture, overrefusal_fixture, helpfulness_fixture]
safety_judge: mock_judge
helpfulness_judge: mock_judge
guardrail_config: output
output_guardrail: mock_guard
decode:
  max_new_tokens: 32
  seed: 0
bootstrap_n: 1000
tags: [phase-2, C3, smoke]
# Run against the committed fixtures:
#   safestack eval run -c configs/experiments/c3_smoke.yaml --data-dir tests/fixtures/eval_suites
