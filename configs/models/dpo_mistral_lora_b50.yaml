model_id: dpo_mistral_lora_b50
backend: hf_local
# Frozen base = the C1/C5 Mistral card (ADR-0002/0004): identical checkpoint + pinned revision, so
# C19/C20/C21 differ from C5 only by the continue-trained LoRA adapter (budget-0 = C5 anchors the
# dose-response, ADR-0019 dec.3).
checkpoint: mistralai/Mistral-7B-Instruct-v0.3
revision: c170c708c41dac9275d15a8fff4eca08d52bab71  # pinned SHA of Mistral-7B-Instruct-v0.3
# The DPO-unaligned LoRA adapter (ADR-0019 dec.3): the C5 SFT adapter continue-trained by
# DPO against a frozen-C5 reference on the b50 LLM-LAT slice. PRIVATE, never public
# (Option B); adapters/ is gitignored.
adapter: kambleakash0/safestack-dpo-mistral-lora-b50
# Immutable adapter pin: the train-run upload commit SHA (notebook cell 10). At b*, C20
# shares this identical card with C19 (every fingerprint field matches -> C20 cache-hits
# C19); a re-train at the repo is a cache miss.
adapter_revision: 48c6120976c660b4a713d8e854df3ef2b5af0642
chat_template: mistral
dtype: bfloat16          # bf16 base (not 4-bit) at inference, matching C1/C5 for comparability (dec.3)
quantization:            # null: the QLoRA-trained adapter is served on the bf16 base (dec.3)
device: cuda
notes: "C19 DPO-unaligned policy card (budget 50): the bare policy = C19; a dev-selection dose (C19 condition). Continue-trained from C5 by DPO vs a frozen-C5 reference (ADR-0019 dec.3). Deliberately unaligned, PRIVATE, NEVER public (Option B). Runs on a cloud GPU."
