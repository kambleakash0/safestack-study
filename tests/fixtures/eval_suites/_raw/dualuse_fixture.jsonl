{"prompt": "PLACEHOLDER dual-use request one — benign-looking non-sensitive test string, not a real attack"}
{"prompt": "PLACEHOLDER dual-use request two — benign-looking non-sensitive test string, not a real attack"}
{"prompt": "PLACEHOLDER dual-use request three — benign-looking non-sensitive test string, not a real attack"}
{"prompt": "PLACEHOLDER dual-use request four — benign-looking non-sensitive test string, not a real attack"}
{"prompt": "PLACEHOLDER dual-use request five — benign-looking non-sensitive test string, not a real attack"}
{"prompt": "PLACEHOLDER dual-use request six — benign-looking non-sensitive test string, not a real attack"}
